

## 1) نظرة عامة 💡
- الاسم: `flutter_hive` — تطبيق Todo بسيط يعتمد على GetX لإدارة الحالة وHive للتخزين المحلي.
- API: يتكامل مع DummyJSON (يمكن استخدام `json-server` محليًا لعمليات CRUD ثابتة).

## 2) المتطلبات المسبقة 🔧
- تثبيت Flutter (مراجعة المسار في PATH). تحقق عبر `flutter doctor`.
- محرر (مثل VS Code أو Android Studio).
- Java / Android SDK لبناء Android، Xcode لبناء iOS (عند الحاجة).

## 3) تثبيت الحزم وتهيئة المشروع ▶️
1. افتح الطرفية في جذر المشروع.
2. شغّل:
   - `flutter pub get` لتحميل الحزم.
3. إذا قمت بتعديل أو إضافة نماذج مع Hive annotations، نفّذ:
   - `flutter pub run build_runner build --delete-conflicting-outputs` لتوليد ملفات `*.g.dart` (TypeAdapters).

## 4) تهيئة Hive في التطبيق 🔁
- افتح `lib/main.dart`، ستجد تهيئة Hive (فتح الصناديق boxes) قبل تشغيل التطبيق.
- تأكد أن `registerAdapter` للنماذج المسجلة (`Category`, `Task`, ...) موجودة قبل فتح الـ box.

## 5) بنية المشروع وشرح الملفات 🔍
- `lib/controllers/` — تحكمات GetX: منطق التطبيق (مثل `task_controller.dart`, `category_controller.dart`).
- `lib/models/` — نماذج البيانات (`category.dart`, `task.dart`، وملفات `*.g.dart` المولدة).
- `lib/screens/` — شاشات التطبيق (قوائم المهام، تصنيفات، تسجيل الدخول، إلخ).
- `lib/services/api/` — ملفات التعامل مع الـ API (نداءات HTTP وعمليات التعامل مع الاستجابة).
- `lib/widgets/` — مكونات واجهة قابلة لإعادة الاستخدام (حوار إضافة مهمة/تصنيف، قوائم، إلخ).
- `lib/helpera/` — إعدادات التطبيق مثل الصفحات، الثيمات، الترجمات، المسارات.

## 6) كيفية التشغيل خلال التطوير ▶️
- لتشغيل على محاكي Android:
  - `flutter run` أو استخدم Run في محرّرك.
- لتشغيل على متصفح (Web):
  - `flutter run -d chrome`.

## 7) سلوك التطبيق مع الفشل في الـ API ⚠️
- عند فشل طلب API (مثلاً 404)، التطبيق يحفظ التغييرات محليًا في `Hive` كـ fallback ويعرض تحذير للمستخدم.
- يمكنك تشغيل `json-server` محليًا إذا رغبت في اختبار CRUD ثابتة وتغيير `apiBaseUrl` إلى `http://localhost:3000`.

## 8) إضافة نموذج / ميزة جديدة (نقطة سريعة) ✍️
1. أنشئ ملف النموذج في `lib/models/` مع annotations لـ Hive (مثلاً `@HiveType` و `@HiveField`).
2. شغّل `build_runner` لتوليد الـ adapter.
3. سجّل الـ Adapter في `main.dart` قبل فتح الـ box.
4. أضف controller جديد في `lib/controllers/` إذا احتاج المنطق.
5. أنشئ شاشة أو ويدجت في `lib/screens/` أو `lib/widgets/`، ثم وصلها بالـ route في `helpera/routes.dart`.

## 9) الاختبارات 🚦
- المشروع يحتوي على ملف اختبار widget في `test/widget_test.dart` كمثال.
- شغّل الاختبارات عبر:
  - `flutter test`

